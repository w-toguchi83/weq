defmodule Weq.Repos.Weqdb2RepoTest do
  use Weq.DataCase, async: true

  #test "connect webdb2" do
  #  assert {:ok, result} = Ecto.Adapters.SQL.query(Weq.Repos.Weqdb2Repo, "SELECT 2")
  #  assert result.rows === [[2]]
  #  assert result.num_rows === 1
  #end
end
